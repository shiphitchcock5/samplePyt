# Language setting
language: python

# Version number
python:
  - 3.4

services:
  - mysql
  - postgres
  - mongodb
  - elasticsearch
  - redis
  - memcache
  - rabbitmq
  - neo4j
  - rethinkdb
  - couchdb
 
build:
  
  ci:
    - python --version
