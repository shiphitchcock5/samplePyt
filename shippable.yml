
# Language setting
language: python

# Version number
python:
  
  - ppy3
  
build:
  pre_ci_boot:
    image_name: drydock/u12pyt
    image_tag: prod
    pull: true
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'
  ci:
    - python --version
