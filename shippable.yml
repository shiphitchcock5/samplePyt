
# Language setting
language: python

# Version number
python:
   - 3.3
   - 3.4
   
env:
  global :
   - M1=one
   

build:
  ci:
    - python --version
    - sleep 8m
    #- echo $key1 and $key2 and $key3
